# --- Vercel 저장소에는 index.html 과 api/ 서버리스 함수만 올립니다 ---
# api/gemini.js 는 Gemini API 키를 브라우저에 노출하지 않기 위한 프록시입니다.
# (키 자체는 저장소가 아니라 Vercel 환경변수 GEMINI_API_KEY 에 있습니다)

# Firebase 로컬 설정/캐시 (Vercel 저장소엔 불필요)
.firebase/
firebase.json
.firebaserc
storage.rules

# 에디터 설정
.vscode/

# 아직 작업 중인 페이지 (완성되면 그때 추가)
FAULT.html
REPORT.HTML

# index.html이 사용하지 않는 로컬 이미지
images/
